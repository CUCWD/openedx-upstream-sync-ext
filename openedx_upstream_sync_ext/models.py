"""
Database models for openedx_upstream_sync_ext.

This plugin currently has no database models.  The fetched upstream score is
an XBlock field stored with the course block, so it does not require a Django
migration or a separate application table.
"""
